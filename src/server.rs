// Step 4: LSP lifecycle, message loop, request/notification routing.
// See docs/design/components/protocol-handler.md

pub fn run(_connection: lsp_server::Connection) -> anyhow::Result<()> {
    Ok(())
}
